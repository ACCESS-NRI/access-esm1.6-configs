# Reuse some experiment level metadata as template variables
-d=../metadata.yaml
# Use environment data as template variables
-d=${PAYU_CURRENT_OUTPUT_DIR}/env.yaml
# Common global metadata
-m=../dataspec.yaml
# Ocean model specific global metadata
--datavar=realm=ocnBgchem
# Extract frequency from filename
--fnregex=oceanbgc-.d-(?P<var>(.*))-(?P<freq>(1monthly|1yearly))-(mean|max|ym).*\.nc
# Files to process
# ${PAYU_CURRENT_OUTPUT_DIR}/ocean/*.nc
